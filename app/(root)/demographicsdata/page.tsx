import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Demographics info Page",
  description: "dhalparagp notice",
};

const page = () => {
  return (
    <div className="flex justify-center items-center">
      <h1>Comming Soon</h1>
    </div>
  );
};

export default page;
