import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "online Forms Link Page",
  description: "dhalparagp notice",
};

const onlineService = () => {
  return (
    <div className="flex justify-center items-center">
      <h1>Comming Soon</h1>
    </div>
  );
};

export default onlineService;
