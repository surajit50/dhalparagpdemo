import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Panchayat meeting Info page",
  description: "dhalparagp notice",
};

const onlineService = () => {
  return (
    <div className="flex justify-center items-center">
      <h1>Comming Soon</h1>
    </div>
  );
};

export default onlineService;
