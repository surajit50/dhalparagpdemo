import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "property Tax details page",
  description: "dhalparagp notice",
};

const page = () => {
  return (
    <div className="flex justify-center items-center">
      <h1>Comming Soon</h1>
    </div>
  );
};

export default page;
